# Registre de coordination — instances Claude parallèles

> Ismael fait souvent tourner **2 instances Claude en parallèle** sur le même repo
> (`/Users/anissa/i-a-infinity-of`, même `main` → Railway). Ce registre évite le travail
> en double et les collisions (migrations, fichiers UI partagés).

## Protocole (résumé — détail dans CLAUDE.md § Coordination)
1. **Avant** de démarrer une feature : `git fetch` + lire ce fichier + `git log origin/main --oneline -20`.
2. **Claimer** sa zone ci-dessous (1 ligne) + commit/push, avant de coder.
3. **Juste avant push/PR** : re-`fetch`, re-lire ce fichier, comparer `git show origin/main:<fichier>` à sa version.
4. **Libérer** la ligne (statut `mergé`) une fois la PR mergée.

## Zones « chaudes » (une seule instance à la fois)
`apps/web/app/inscription/**` · pages dossier UI (`app/(dashboard)/dossiers/**`) ·
`apps/web/features/attendance/**` · `app/(dashboard)/page.tsx` (home) · `supabase/migrations/**` (numéros).

## Claims actifs

| Instance | Module / chemins | Branche | Depuis | Statut |
|----------|------------------|---------|--------|--------|
| Opus (bpf-kpi) | **KPI de synthèse sur la page BPF** (demande Ismael) : rangée de 4 `StatCard` en tête de `bpf/page.tsx` — Produits HT (`financial.totalCents` + dossiers/actions), Stagiaires distincts, Heures dispensées, Formateurs intervenus (internes/externes). Réutilise `loadBpfAggregates` (aucune requête ajoutée). **Sans migration.** | feature/bpf-kpi | 2026-07-16 | mergé (PR #124) |
| Opus (facture-emise-bleu-badge) | **Facture émise en bleu + badge Gestion = en retard** (demande Ismael) : (1) statut `issued`/« émise » passé du ton `warning` (orange) au ton `info` (**bleu**) dans `invoice-status-control.client.tsx` + filet/fond de ligne bleu dans `factures/page.tsx`. (2) Le badge « à traiter » de la sidebar (groupe Gestion) ne compte plus toutes les factures non payées mais **uniquement les factures en retard** (`overdue`) — une facture simplement émise ne fait plus rester le « 1 ». Renommage `invoicesUnpaid`→`invoicesOverdue` (sidebar-rail + server, query `.eq('status','overdue')`). **Sans migration.** | feature/facture-emise-bleu-badge | 2026-07-16 | mergé (PR #123) |
| Opus (qualiopi-inline-resolution) | **Résolution inline des indicateurs bloquants + barre onglets** (demande Ismael) : (1) `scrollbar-hide` (globals.css) appliqué à `TabsNav` → fin de la barre de défilement horizontale moche. (2) Sur `dossiers/[id]/qualiopi/page.tsx`, les indicateurs bloquants **questionnaire** (I5/I10 positionnement, I15/I23 evaluation_acquis, I26 satisfaction_chaud, I27 froid) se résolvent **inline** (réutilise `AssignLearner` — select modèle + « Affecter à l'apprenant »), comme I21 formateur. Les autres (émargements, programme, preuve) gardent le lien. **Sans migration.** | feature/qualiopi-inline-resolution | 2026-07-15 | mergé (PR #122) |
| Opus (migration-history-placeholders) | **Débloque le workflow CI « DB migrations (prod) »** : la prod a les versions 0108/0110 dans son historique mais sans fichier local → `supabase db push` refusait TOUTE migration (« Remote migration versions not found in local »). Ajout de 2 fichiers **NO-OP** `0108_history_sync_placeholder.sql` + `0110_history_sync_placeholder.sql` (commentaires seuls) pour aligner le repo sur l'historique distant. Versions déjà appliquées en prod → non re-jouées. **Ne touche pas au remote** (pas de repair). | fix/migration-history-placeholders | 2026-07-15 | ANNULÉ (PR #120 revert) — approche erronée, restaure l état sain |
| Opus (formateur-photo-description) | **Photo + description formateur** (demande Ismael, volets 2-3/3) : **migration 0113** (`trainers.photo_path` + bucket public `trainer-photos` + policies admin/owner). Upload photo (`photo/upload/route.ts`), édition photo + description (bio existante) via `profile-edit.tsx` + `profile-actions.ts` (`updateTrainerBio`), carte « Profil public » sur la fiche. Affichage : photo dans l'en-tête fiche + **cartes de la liste `/formateurs`** (catalogue) ; bio affichée sur la fiche. **Migration 0113.** | feature/formateur-photo-description | 2026-07-15 | mergé (PR #119, migration 0114) |
| Opus (formateur-contrat-ia) | **Contrat de sous-traitance formateur généré par IA** (demande Ismael, volet 1/3) : `features/documents/generate-trainer-contract.ts` (prompt contrat de sous-traitance conforme droit FR, clauses objet/durée/indépendance-non-subordination/rémunération/RC pro/confidentialité/RGPD/PI/résiliation/litiges) + action `formateurs/[id]/contract-actions.ts` (`generateTrainerContract` → doc standalone `kind='trainer_contract'`, `metadata.trainer_id`, habillé `wrapGeneratedHtml`) + bouton `contract-generate.tsx` sur la fiche formateur → aperçu brandé + signature via le panneau existant (email formateur). **Sans migration.** Suite prévue : photo + description formateur (volets 2-3). | feature/formateur-contrat-ia | 2026-07-15 | mergé (PR #118) |
| Opus (reclamations-statut-couleur) | **Réclamations : ligne colorée par statut + icône** (demande Ismael) : sur `reclamations/page.tsx`, chaque ligne prend un **fond + bordure gauche + icône** selon le statut — résolue = **verte + ✓**, ouverte = rouge + alerte, en cours = ambre + horloge, clôturée = neutre + archive. Map `STATUS_VIS`. **Sans migration.** | feature/reclamations-statut-couleur | 2026-07-15 | mergé (PR #117) |
| Opus (notifications-categories-clear) | **Notifications : filtre par catégorie + retrait au clic** (demande Ismael) : (1) puces de **filtre par catégorie** (template_code + compteurs, `?type=`) sur `notifications/page.tsx` ; (2) la page n'affiche plus que les **non-lues** et chaque notif est **marquée lue au clic** (`markNotificationRead(id)` + `notif-item.client.tsx`) → disparaît. Cohérence cloche : `notifications-bell.tsx` ne marque plus TOUT lu à l'ouverture (retrait par clic + bouton « Tout marquer lu ») ; `topbar.tsx` source cloche filtrée `read_at IS NULL`. **Sans migration** (réutilise `read_at` 0088). | feature/notifications-categories-clear | 2026-07-15 | mergé (PR #116) |
| Opus (notifications-classees) | **Notifications classées par période** (demande Ismael) : `notifications/page.tsx` passait d'une liste plate à un **classement par période** (Aujourd'hui / Hier / 7 derniers jours / Plus ancien), chaque groupe en section avec compteur ; ajout d'un **badge de type** (label `NOTIF_META`) sur chaque ligne. **Sans migration.** | feature/notifications-classees | 2026-07-15 | mergé (PR #115) |
| Opus (doc-legal-org-info) | **Docs légaux : injecter l'identité de l'organisme** (demande Ismael) : `orgInfo()` de `documents-legaux/actions.ts` ne chargeait que `name` + `declaration_activite` → l'IA générait Règlement/CGV/Livret avec SIRET/adresse/représentant/contact vides. Étendu `OrgInfo` (legalName, siret, address, representative(+title), email, phone) + `buildLegalPrompt` (bloc identité + consigne « reprendre ces valeurs ») ; `orgInfo()` charge tous les champs + compose l'adresse JSONB ; PDF `validateLegalDoc` reçoit la vraie adresse. Test build-legal-prompt OK. **Sans migration.** À REGÉNÉRER côté user pour les docs déjà créés. | feature/doc-legal-org-info | 2026-07-15 | mergé (PR #114) |
| Opus (doc-branding-capsule) | **Branding des documents générés** (demande Ismael) : l'aperçu/impression d'un document HTML (Règlement intérieur, docs IA/modèles) n'avait ni logo ni couleur. Ajout dans `documents/[id]/apercu/page.tsx` + `_components/editable-document.tsx` : en-tête avec **logo de l'organisme** (repli logo Capsule `logo-capsule-full.png`) + nom, **filet orange (#f97316, couleur de marque) sur les marges** (bord haut + gauche), pied « Généré avec Capsule IA », impression des couleurs (`print-color-adjust: exact`). Réutilise `loadOrgLogoDataUri`. **Sans migration.** | feature/doc-branding-capsule | 2026-07-15 | mergé (PR #113) |
| Opus (sessions-embed-free) | **Fix DÉFINITIF « erreur base de données » /sessions** : le repli #106 gardait les embeds `dossier:dossiers(...)` ; un cache de schéma PostgREST périmé (post-0106) casse la résolution des relations de `sessions` → l'embed échoue aussi. Réécriture de `sessions/page.tsx` **sans aucun embed** : sessions lues à plat (+ repli sans `formation_id`), puis dossiers/apprenants/formations rattachés par requêtes `.in()` séparées (tables inchangées → insensibles au cache). La page charge quoi qu'il arrive ; bannière d'erreur seulement si `sessions` est réellement inaccessible. **Sans migration.** | fix/sessions-embed-free | 2026-07-15 | mergé (PR #112) |
| Opus (remove-dossier-modules-tab) | **Suite suppression Modules** (demande Ismael) : retire l'onglet **Modules** de la fiche dossier — entrée `modules` dans `shared/components/layout/tabs-nav.tsx` (utilisé uniquement par la fiche dossier), tuile + compteur `dossier_modules` + import `ClipboardList` + texte dans `dossiers/[id]/page.tsx`, et suppression de `dossiers/[id]/modules/page.tsx`. **Sans impact données** (`dossier_modules` conservée, utilisée par le wizard/heures). Sans migration. Zone chaude `dossiers/[id]` (édits chirurgicaux, aucun claim concurrent). | chore/remove-dossier-modules-tab | 2026-07-14 | mergé (PR #111) |
| Opus (pieces-couleurs-ergonomie) | **Lisibilité vérification des pièces** (demande Ismael) : dans `prospects/[id]/prospect-detail-actions.tsx`, refonte du rendu des pièces en **cartes colorées** avec icône de statut — **manquante/refusée = rouge fort**, à vérifier = ambre, validé = vert, optionnel non fourni = neutre. Ajout d'un **bandeau récap** en tête (X manquantes/refusées bloquantes · Y à vérifier · toutes validées). Actions inchangées (Voir/Valider/Refuser). **Sans migration.** Zone `prospects/[id]/prospect-detail-actions.tsx` (composant, ≠ page.tsx du restyle). | feature/pieces-couleurs-ergonomie | 2026-07-14 | mergé (PR #110) |
| Opus (agenda-defaut-semaine) | **Agenda par défaut en Semaine** (demande Ismael) : une ligne dans `agenda/page.tsx` — défaut de `view` passé de `liste` à `semaine` (`?view=liste` reste accessible via la bascule). **Sans migration.** | fix/agenda-defaut-semaine | 2026-07-14 | mergé (PR #108) |
| Opus (sessions-formation-id-fallback) | **Fix « erreur base de données » sur /sessions** : après migration 0106 (colonne `sessions.formation_id`), le cache de schéma PostgREST pouvait rester périmé (NOTIFY reload 0107 éphémère, manqué si projet en reprise de pause) → la requête sélectionnant `formation_id` échouait et la page affichait « Impossible de charger les sessions ». Ajout d'un **repli** dans `sessions/page.tsx` : si la requête avec `formation_id` échoue, on recharge SANS cette colonne (liste utilisable, formations de groupe résolues quand le cache revient) ; la bannière d'erreur ne s'affiche plus que si le repli échoue aussi. **Sans migration.** | fix/sessions-formation-id-fallback | 2026-07-14 | mergé (PR #106) |
| Opus (remove-formation-programme) | **Suite suppression Modules** (demande Ismael) : retire la section « Programme » de la fiche formation `formations/[id]/page.tsx` (carte Programme + KPI « Modules » + requête `formation_modules` + import `BookOpen`), qui renvoyait au catalogue de modules supprimé (#103) et restait toujours vide. Grille KPI 4→3. **Sans impact** (données conservées), sans migration. | chore/remove-formation-programme | 2026-07-13 | mergé (PR #105) |
| Opus (remove-modules-page) | **Suppression page Modules** (demande Ismael) : `/modules` (catalogue tarifaire) était un cul-de-sac — aucun moyen de créer un module (form formation sans section modules, aucun insert dans `app.modules`). Suppression des 3 fichiers `app/(dashboard)/modules/**` + entrée sidebar (Catalogue) + mapping `permissions.ts` `['/modules','catalogue']` + import icône `BookOpen`. **Aucun impact** : table `app.modules`/`dossier_modules`/`formation_modules` conservées, wizard dossier charge les modules via sa propre requête `formation_modules`. **Sans migration.** | chore/remove-modules-page | 2026-07-13 | mergé (PR #103) |
| Opus (fiche-financeur) | **Fiche détail financeur** : la carte financeur (liste `/financeurs`) n'était pas cliquable (aucune page `[id]`). Ajout `financeurs/[id]/page.tsx` (coordonnées, KPIs dossiers/total financé, liste des dossiers financés, édition + archivage gardé) + `financeurs/[id]/actions.ts` (`updateFunder`/`deleteFunder`, RLS `funders_update`, requireAccess catalogue/manage) ; carte de la liste rendue cliquable (Link). **Sans migration**. Zone `financeurs/**`. | feature/fiche-financeur | 2026-07-13 | mergé (PR #100) |
| Opus (planifier-session-hors-dossier) | **Découvrabilité sessions** : la page `/sessions` n'avait aucun bouton de création et son état vide n'évoquait que « depuis un dossier » → l'utilisateur croyait le formulaire formation/session disparu (il existe : `/formations/nouvelle` = form SoSafe, `formations/[id]` = `GroupSessionForm`). Ajout page `/sessions/nouvelle` (sélecteur de formation + réutilise `GroupSessionForm`, `defaultOpen`+`redirectTo` ajoutés au composant, backward-compatible) → créer une session de groupe **sans dossier**. Bouton « Planifier une session » sur `/sessions` (en-tête + état vide, `ManageOnly catalogue`) + message corrigé. **Sans migration**, réutilise `createFormationSession`. | feature/planifier-session-hors-dossier | 2026-07-12 | mergé (PR #99) |
| Opus (facturation-montant-total) | **Fix facturation** : un dossier sans `total_amount_cents` n'affichait aucun bouton « Facturer » (toutes lignes payeur à 0 €) tout en invitant à cliquer « Facturer ci-dessus » → impossible de facturer, aucune page pour saisir le montant après création. Ajout `dossiers/[id]/facturation/actions.ts` (`setDossierTotalAmount`, RLS staff, requireAccess billing/manage) + carte de saisie/édition du montant total HT sur `facturation/page.tsx` (visible quand montant manquant, éditable via `<details>` sinon) + message d'état corrigé. **Sans migration** (colonne `total_amount_cents` existante). Zone `dossiers/[id]/facturation/**` uniquement. | fix/facturation-montant-total | 2026-07-12 | mergé (PR #97) |
| Opus (agenda-vue-liste) | **Additif sur #89 (déjà mergé)** : bascule Liste/Semaine dans `/agenda/page.tsx` (param `?view=liste|semaine`, défaut **liste**). Vue liste « façon Google agenda » : événements groupés par jour (jour + date, aujourd'hui en orange), heure début–fin + titre complet + lieu + icône Meet + pastille couleur, scrollable. La grille hebdo #89 reste intacte (enveloppée dans `view==='semaine'`). **Sans migration**, purement additif (aucune logique data touchée, réutilise days/allDayByDay/timedByDay). | feature/agenda-liste | 2026-07-10 | mergé (PR #91) |
| Opus (espace-apprenant-mobile-perso) | **Fix espace apprenant** : (1) **Personnalisation KO / fuite RGPD** — 5 appels `.rpc()` (`_lib.ts`, `resources.ts`, `exercises.ts`, `actions.ts`) oubliaient `.schema('app')` → PostgREST cherchait `public.<fn>` → échec → **fallback mock « Alice » affiché à tous**. Ajout `.schema('app')` partout + **suppression du fallback mock** (token invalide → notFound, jamais les données d'un tiers). (2) **Responsive mobile** : `espace-sidebar.tsx` sidebar `hidden lg:flex` + barre mobile + drawer ; `layout.tsx` flex-col→row ; padding pages `px-4 sm:px-6 lg:px-8`. **Sans migration.** Rebasé sur ba6907f (perso organisme) : branding = logo/nom organisme conservé dans sidebar + drawer. | fix/espace-apprenant-mobile-perso | 2026-07-12 | mergé (PR #95) |
| Opus (agenda-google-grille) | **Décision produit (demande explicite Ismael)** : la sidebar « Agenda » repointe vers `/agenda` (agenda Google perso de l'utilisateur), PAS `/planning`. `/planning` reste le calendrier des sessions OF (intact). `/agenda/page.tsx` reconstruit en grille hebdo colorée (couleurs Google conservées via colorId + palette + couleur d'agenda) ; enrichissement `shared/lib/integrations/google-calendar-client.ts` (listAgenda + couleurs). **Ne pas re-pointer « Agenda » vers /planning** sans en parler. Sans migration. | feature/agenda-grille-couleurs | 2026-07-10 | actif |
| Opus (documents-standalone) | Documents sans dossier : `documents/actions.ts` (uploadStandaloneDocument → bucket documents, dossier_id NULL ; attachDocumentToDossier), route `api/documents/[id]` (download signé staff), composants `document-tools.tsx` (bouton Téléverser + select Rattacher), page `/documents` (bouton + lignes sans dossier). **Sans migration** (dossier_id déjà nullable). | feature/documents-standalone | 2026-07-03 | actif |
| Opus (planning-lun-sam) | Calendrier hebdo natif `planning/page.tsx` étendu **lundi→samedi** (6 colonnes) ; entrée sidebar « Agenda » repointée vers `/planning` (calendrier natif, au lieu de la page Google Agenda). Sans migration. | feature/planning-lundi-samedi | 2026-06-29 | actif |
| Opus (restyle-demande-detail) | Restyle page détail demande `prospects/[id]/page.tsx` : hero chaleureux (dégradé orange/rose + avatar initiales), police éditoriale Fraunces (titres), cartes infos colorées (bleu/ambre/rose/émeraude), fiche besoin en ambre, historique en timeline orange ; back link → /prospects. **Pas de violet « IA »**, logique inchangée. Sans migration. | feature/restyle-demande-detail | 2026-06-28 | actif |
| Opus (demandes-unifiees) | **ZONE CHAUDE CRM** Page unique « Demandes » : fusion Nouvelles demandes + Pré-inscriptions sur `/prospects` (restyle coloré + KPIs), `/prospects/nouvelles` redirige, sidebar 1 entrée. **Auto-conversion** : validation des pièces (`validateProspectDemande`) → conversion auto en dossier via helper extrait `features/crm/prospect-conversion/convert-core.ts` (réutilisé par `convertProspect`). Sans migration. | feature/demandes-unifiees | 2026-06-28 | actif |
| Opus (inscription-situation-first) | **ZONE CHAUDE** Tunnel individuel réagencé : Situation & financement EN PREMIER (typologie), puis Vous → Formation → Fiche besoin → Documents. Fiche besoin **adaptée à la typologie** (question spécifique salarié/demandeur/indépendant/particulier, `needsAnalysisFields` prop `typology` + champ `typologyContext`, schema). | feature/inscription-situation-first | 2026-06-28 | actif |
| Opus (fix-audit-no-id) | **Fix prod bloquant** : `audit.audit_row()` faisait `NEW.id`/`OLD.id` ; le trigger tg_audit est posé sur `dossier_trainers` (PK composite, sans `id`) → « record "new" has no field "id" » à la création de dossier avec formateur. **Migration 0104** : résolution sûre de l'id (EXCEPTION undefined_column → NULL). | fix/audit-row-no-id | 2026-06-27 | actif |
| Opus (bpf) | **BPF** (Bilan Pédagogique et Financier, Cerfa 10443) : domaine pur `features/bpf/bpf.ts` (ventilation produits HT par origine financeur → lignes BPF + tests), page `app/(dashboard)/bpf` (cadres B produits / C pédagogique / D formateurs, sélecteur d'année, impression PDF), lien sidebar (Qualité). Produits = factures émises dans l'année. **Sans migration.** | feature/bpf | 2026-06-27 | actif |
| Opus (veille-amelioration) | Module **Veille & amélioration continue** (Qualiopi crit. 6) : **migration 0102** (`veille_entries` + `improvement_actions` + RLS + pgTAP 0102), page `app/(dashboard)/amelioration-continue` (registre veille + plan d'actions), actions create/update, **réclamations ouvertes → bouton « créer une action »** (lien `complaint_id`), lien sidebar (Qualité). | feature/veille-amelioration-continue | 2026-06-27 | repris par Claude (amelioration-continue) le 2026-09-11 |
| Opus (attestations-auto) | Auto-envoi documents formation : **certificat de réalisation** ajouté à l'email fin de dossier (tous dossiers, en plus de l'attestation de fin) ; **attestation de démarrage** (variante `entree` du générateur attestation + route `api/dossiers/[id]/attestation-entree.pdf`) envoyée aux **présents** (1re présence émargée) via tâche cron `runStartAttestation` (dédup email_log kind=attestation_demarrage). Templates `endOfTrainingEmail`(+certificat)/`startOfTrainingEmail`. **Sans migration.** | feature/attestations-auto | 2026-06-27 | actif |
| Opus (tracabilite-docs-globale) | Vue **globale** traçabilité documents `app/(dashboard)/tracabilite/page.tsx` (lecture `resource_access_log`, KPIs ouvertures/téléchargements/par apprenant, filtre dossier) + liens sidebar (Suivi : Emails + Traçabilité docs). **Sans migration** (réutilise resource_access_log déjà alimenté). | feature/document-access-tracking | 2026-06-27 | actif |
| Opus (email-tracking) | Traçabilité emails via **webhooks Resend** : **migration 0101** (email_log + delivered/opened/clicked/bounced/complained + open_count/click_count), route `app/api/webhooks/resend` (vérif signature Svix + MAJ par provider_id), env `RESEND_WEBHOOK_SECRET`, page `/emails` (colonne Suivi + KPI Ouverts). | feature/email-tracking-resend | 2026-06-27 | actif |
| Opus (fix-org-resolution) | **Fix multi-tenant** : `createFunder` et `createLearner` résolvaient l'org via « première org » (service-role) → création dans une org arbitraire, invisible dans la liste RLS de l'utilisateur. Remplacé par `resolveAdminOrgId(user.id)` (membership connecté), comme `createFormation`. **Sans migration.** | fix/create-actions-org-resolution | 2026-06-27 | actif |
| Opus (financeur-multi-type) | Financeur multi-types : **migration 0099** (`funders.kinds` funder_kind[], backfill ; `kind` = type principal conservé). De-mock création `financeurs/nouveau` (Server Action `createFunder` réelle + insert), cases catégorie en multi-sélection. | feature/financeur-multi-type | 2026-06-27 | actif |
| Opus (fiches-besoin-consult) | Page staff de consultation des fiches besoin : `app/(dashboard)/fiches-besoin/page.tsx` (regroupées par formation, réponses détaillées, sources apprenants `questionnaire_responses` positionnement + inscrits `prospects.needs_analysis`) + lien sidebar (groupe Qualité). **Sans migration.** | feature/fiches-besoin-consultation | 2026-06-26 | actif |
| Opus (fiche-besoin-inscription) | **ZONE CHAUDE** Fiche besoin EN LIGNE dans le formulaire d'inscription : **migration 0097** (`prospects.needs_analysis` JSONB), composant `app/inscription/needs-analysis-fields.tsx`, `schema.ts` (`needsAnalysisSchema` sur prospect + employee), étape fiche besoin (individuel + 1/salarié dans CompanyFlow), `actions.ts` (stocke `needs_analysis`). | feature/fiche-besoin-inscription | 2026-06-26 | actif |
| Opus (fiche-besoin-sans-dossier) | **migration 0096** (`questionnaire_assignments`/`responses`.`dossier_id` nullable, RLS inchangée car org-scopée). Token fiche besoin `dossierId` optionnel, page `/questionnaire/besoin/[token]` gère le cas sans dossier, helper `sendNeedsAnalysisForLearner`, hook `apprenants/nouveau/actions.ts` (email à la création) + filet cron apprenants. | feature/fiche-besoin-sans-dossier | 2026-06-26 | actif |
| Opus (fiche-besoin) | Fiche besoin (questionnaire positionnement) envoyée auto à l'apprenant dès rattachement à un dossier : `shared/lib/needs-analysis-token.ts`, helper `features/questionnaire/needs-analysis.ts` (template système + assignation + token + email, idempotent), page publique `app/questionnaire/besoin/[token]/**`, email `templates.ts` (`needsAnalysisEmail`), hook `dossiers/nouveau/actions.ts`, filet cron `transactional-emails`. **Sans migration** (réutilise questionnaire_*). | feature/fiche-besoin-auto | 2026-06-26 | mergé (PR #62) |
| Opus (inscription-entreprise) | **ZONE CHAUDE** Inscription groupée par une entreprise : bascule mode individuel/entreprise dans `app/inscription/inscription-form.tsx`, `schema.ts` (`companyEnrollmentSchema` + `employeeSchema`), `actions.ts` (`submitCompanyEnrollment` → 1 prospect/salarié partageant entreprise+référent+formation+financement, emails confirmation+notif). **Sans migration** (réutilise `prospects`). Additif, ne touche pas le parcours individuel. | feature/inscription-entreprise-salaries | 2026-06-26 | actif |
| Opus (multi-financement) | **ZONE CHAUDE** Multi-financement + anti-double-facturation : domain pur `features/billing/domain/billing-plan.ts` (plan par payeur + garde `canBill`), wizard `dossiers/nouveau/**` (schema `funders[]`, step 3 lignes financeurs répétables + reste à charge), vue `dossiers/[id]/facturation/page.tsx` (récap par payeur, fix bug `dossiers.funder_id` inexistant), `factures/actions.ts createInvoice` (choix payeur + garde serveur), `factures/nouvelle/page.tsx` (pré-remplissage payeur/montant restant). **Sans migration** (réutilise `dossier_funders`). | feature/multi-financement-facturation | 2026-06-26 | mergé (PR #60) |
| Opus (remove-afest) | Retrait modalité **AFEST** : **migration 0095** (recrée enum `app.training_modality` sans `afest`, convertit 5 colonnes, drop/recrée vues `v_dossiers_overview`+`attendance_consolidated`). Code transverse : listes/enums modalités, libellés UI (zones chaudes `dossiers/nouveau`, `inscription`), PDF, email, `panels-for-modality`, types générés. | (push direct main) | 2026-06-26 | mergé |
| Opus (doc-categories) | Catégories de modèles de documents : **migration 0093** (`document_categories` + RLS + `document_templates.category_id`, appliquée prod+staging), actions create/delete catégorie, select dans l'éditeur, regroupement par catégorie dans `documents/modeles`. Zone docs (mienne). | feat/document-categories | 2026-06-18 | mergé (PR #57) |
| Opus (fiche-apprenant) | Fiche détail apprenant (hub) : `app/(dashboard)/apprenants/[id]/**` (page + header + cards + edit + action `updateLearner`) ; débranchement stub `href` dans `apprenants/page.tsx`. Réutilise `AnonymizeAction` + pages `/dossiers/[id]`. **Sans migration.** Inclut un hotfix import `dossiers/nouveau/page.tsx` (main ne compilait plus). | feature/fiche-detail-apprenant | 2026-06-18 | mergé (push direct main) |
| Opus (docs-phase3) | Docs phase 3 final : (A) envoi PDF par email (`dossiers/[id]/documents` email-actions, attache PDF persisté via SMTP), (B) héritage formation→modèle (**migration 0092** `document_templates.formation_id`, select dans éditeur `documents/modeles`, picker filtré), (C) génération document par IA (`features/documents/templates/generate-with-ai.ts` + action + UI dans onglet documents). Zone docs (déjà mienne). | feat/documents-phase3-final | 2026-06-18 | mergé (PR #54) |
| Opus (emargement) | **ZONE CHAUDE** Émargement demi-journée branché & modality-aware : de-mock `app/(dashboard)/dossiers/[id]/emargements/[sessionId]/**` + `app/(formateur)/emarger/[id]/**`, `features/attendance/**` (action `ensureSessionSheets` via RPC `materialize_attendance_slots`, `panelsForModality`, conversion legacy `'full'`), composants réels existants branchés. Spec : `docs/superpowers/specs/2026-06-18-emargement-demi-journee-design.md`. Sans migration (réutilise RPC 0082). | feat/emargement-demi-journee | 2026-06-18 | mergé (PR #59) |
| Opus (fk-deferrable) | Fix prod : FK `dossier_status_history.dossier_id` rendue DEFERRABLE (**migration 0094**) — le trigger `tg_dossier_transitions` insérait l'historique avant l'existence du dossier (ERREUR 23503, création de dossier cassée). Déjà appliqué prod+staging. | fix/dossier-history-fk-deferrable | 2026-06-18 | mergé (PR #58) |
| Opus (e-signature) | E-signature de documents par lien token : helper `shared/lib/document-signature-token.ts`, demande staff `documents/[id]/apercu/signature-actions.ts` + panneau, page publique `(apprenant)/signer/document/[token]` + canvas + action soumission, statut/audit via `document_signatures` existant. **Sans migration.** | feat/document-esignature | 2026-06-18 | mergé (PR #52) |
| Opus (doc-templates) | Système de modèles de documents éditables (façon sosafe) : **migration 0089** (`document_templates.content_html` + `documents.content_html`), moteur de variables/rendu `features/documents/templates/**`, éditeur `(dashboard)/documents/modeles/**`, génération depuis modèle dans `dossiers/[id]/documents/**` (zone chaude — je viens de la livrer en PR #47), page aperçu/impression `(dashboard)/documents/[id]/apercu`. **Migration 0089 appliquée prod+staging.** | feat/document-templates | 2026-06-18 | mergé (PR #48) |
| Opus (cable-dossier) | Câblage réel création de dossier (de-mock wizard `dossiers/nouveau/**` — zone chaude : schema Zod + Server Action `createDossierAction` → RPC `save_dossier`, options Supabase réelles learners/formations+modules/trainers/funders, form RHF 3 étapes) + onglet documents actionnable (`dossiers/[id]/documents/page.tsx` : boutons générer/télécharger via routes PDF existantes). **Sans migration.** | feat/cable-dossier-creation | 2026-06-18 | mergé (PR #47) |
| Opus (logo-capsule) | Nouveau logo Capsule IA (capsule + toque, détouré) partout : assets `public/logo-capsule{,-full}.png` + favicon `app/icon.png` ; `shared/ui/logo.tsx` (icône carrée → sidebars OF + apprenant + login + header formateur) ; 3 templates email. **Sans migration.** | (push direct main) | 2026-06-18 | mergé |
| Opus (lien-inscription) | Lien d'inscription niveau organisme (`/inscription?org=<id>`) + page `parametres/inscription` (lien + snippet bouton) : `app/inscription/page.tsx` (zone chaude), `features/catalog/public-catalog.ts`, `parametres/**`. **Sans migration, mode lien (pas d'iframe).** | feature/lien-inscription-org | 2026-06-15 | mergé (PR #36) |
| Opus (qst-module) | Module Questionnaires F-QST-01/02/04/06/07/08 : éditeur templates (`(dashboard)/questionnaires/**`), affectation apprenant + saisie manuelle (`dossiers/[id]/questionnaires/**` — zone chaude), export PDF, stats, génération IA. **Sans migration.** | feature/questionnaires-module | 2026-06-15 | mergé (PR #34) |
| Opus (F-FAC) | export CSV + payeur subrogation | feature/factures-export | 2026-06-15 | mergé (PR #32) |
| Opus (F-AUT-08) | logs d'envoi email | feature/email-log | 2026-06-15 | mergé (PR #30) |
| Opus (real-login) | Vraie auth email+mot de passe : de-mock `app/(auth)/login/**` (form + actions `login`/`logout` via `signInWithPassword`) + bouton déconnexion dans `shared/components/layout/topbar.tsx`. **Sans migration.** | feature/real-login | 2026-06-17 | mergé (PR #41) |
| Opus (rgpd) | Effacement RGPD (anonymisation apprenants+prospects) : **migration 0087** (`anonymize_learner`/`anonymize_prospect` + `prospects.anonymized_at`), `app/(dashboard)/rgpd/**`, branchement boutons listes apprenants/prospects. **Transverse PII** (lecture seule des autres zones). | feature/rgpd-effacement | 2026-06-15 | mergé (PR #39) |
| _(exemple)_ | `apps/web/app/inscription/**` | `feature/xxx` | 2026-06-14 | libéré |
| Opus (de-mock) | financeurs (de-mock) | feature/financeurs-reels | 2026-06-14 | mergé (PR #21) |
| Opus (de-mock) | de-mock dashboards (apprenants/membres/organisation/questionnaires) | feature/demock-dashboards | 2026-06-15 | mergé (PR #24) |
| Opus (de-mock) | /qualiopi + /mes-sessions | feature/demock-qualiopi-sessions | 2026-06-15 | mergé (PR #26) |
| Opus (éditable+KPI) | paramètres éditables + KPI cliquables | feature/parametres-editables, feature/kpi-clickable | 2026-06-15 | mergé (PR #27, #29) |
| Opus (catalog+dossier) | `dispatch-events/route.ts` + migrations 0073/0074 (garde-fous dossier) ; déjà mergé : prix module, contrat formateur, snapshot prix, catalogue formations, /inscription (org-scopé) | (push direct main) | 2026-06-14 | mergé |
| Opus (catalog+dossier) | `shared/ui/logo.tsx` → wordmark « Capsule IA / par IA infinity » (PR #22 n'avait changé que l'alt, image PNG dit encore IA INFINITY) | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | F-DOC-09 certificat de réalisation : `features/documents/generate-certificat-pdf.ts` + `api/dossiers/[id]/certificat.pdf` + stub espace câblé (sous-lot 3.4, reste = instance //) | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | F-EMA-01 créneaux demi-journée : migration 0082 (RPC `materialize_attendance_slots` + trigger sessions + backfill, DB-only). UI émargement (emarger/mes-sessions) = instance // | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | F-EMA-08 alerte signature manquante : cron `transactional-emails` + `shared/lib/email/attendance-reminder.ts` (notif in-app + email, dédup via notifications, sans migration) | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | F-EMA-04 QR par créneau (backend) : `shared/lib/qr.ts` + endpoint `api/attendance/[sheetId]/qr` (PNG du lien de signature personnalisé, auth RLS, sans migration). UI emarger (`<img>`) = instance // | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | Réconciliation Zoom↔demi-journée : `cron/zoom-sync/route.ts` + `features/attendance/half-day-window.ts` (émargement Zoom par créneau matin/après-midi, sans migration) | (push direct main) | 2026-06-15 | mergé |
| Opus (form-formation) | Formulaire création/édition formation façon SoSafe (5 sections accordéon, extras en `metadata.catalog`) : `features/formations/**`, `app/(dashboard)/formations/nouvelle`+`[id]/edit`, `shared/ui/{accordion-section,rich-text}`. **Sans migration.** | feature/formation-form-sosafe | 2026-06-15 | mergé (PR #28) |
| Opus (form-formation) | Gaps Espace Apprenant F-APP-11 (questionnaire persistant), F-APP-05 (lien Zoom dashboard/sessions), F-APP-04 (signature docs depuis l'espace) : `app/(apprenant)/espace/[token]/**` (questionnaire, sessions, documents) + RPC éventuel. Zone partagée espace = coordonner. **Sans migration.** | feature/espace-apprenant-gaps | 2026-06-15 | mergé (PR #31) |
| Opus (catalog+dossier) | F-FOR-10 satisfaction formateur : migration 0084 (enum), token + `/questionnaire/formateur/[token]`, cron `transactional-emails` (assignation+email), email isolé. Réutilise infra questionnaire. | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | Module 3.12 Reporting (couche données) : migration 0085 vue `v_org_reporting` (7 KPIs : pipeline prospects, dossiers actifs, conformité Qualiopi %, retour questionnaires %, heures réelles vs prévues, CA facturé/encaissé/à encaisser, réclamations). UI home/reporting = instance // | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | RBAC 4.1 : rôles `commercial` (→is_staff) + `referent` (lecture seule, policies SELECT additives) — migration 0086. ⚠️ enum `member_role` étendu → UI `parametres/membres` (instance //) à mettre à jour pour exposer les rôles. | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | 2FA admin (4.1) : gate enforcement dans `shared/lib/supabase/middleware.ts` (AAL2 requis pour admin/owner → redirige vers le flux `/parametres/securite/mfa` existant). Piloté par env `ENFORCE_ADMIN_MFA` (**OFF par défaut** → inerte). Sans migration, sans doublon. | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | Suite audit (disjoint) : pages `/reporting` (consomme `v_org_reporting`) + `/notifications` (table `notifications`) + 2 liens sidebar ; feedback `apprenants/nouveau` (erreurs+loading) ; empty states `apprenants`+`entreprises`. **Sans migration.** Évite home/documents/planning/émargement (instance //). | (push direct main) | 2026-06-15 | mergé |
| Opus (catalog+dossier) | Suite audit 2 (disjoint) : `/reclamations/nouvelle` (saisie réelle + action) → fin du lien mort ; `shared/ui/form-submit.tsx` ; feedback `entreprises/nouvelle` ; sidebar « Réglages »→« Paramètres ». **Sans migration.** | (push direct main) | 2026-06-15 | mergé |
| Opus (fix-404) | Liens 404 « page not found » : liens mock codés en dur (`/dossiers/d-1…`) dans home `(dashboard)/page.tsx`, `/documents`, `/planning`, wizard `nouveau/step-3` → repointés sur listes réelles ; onglet Émargements de-mocké (liste réelle `attendance_sheets` + empty state, lecture seule). **Sans migration.** Détail émargement (câblage composants réels) laissé à l'instance attendance. | fix/demock-404-links | 2026-06-15 | actif |
| Opus (fix-404) | De-mock `/documents` (app.documents + onglets dérivés du statut réel) et `/planning` (calendrier hebdo app.sessions). **Sans migration.** | feature/demock-documents-planning | 2026-06-15 | actif |
| Opus (prod-hardening) | Gate auth dans `shared/lib/supabase/middleware.ts` (visiteur non connecté → /login, routes publiques exemptées) + fix typo `app.memberships`→`members` (page Zoom) + workflow CI `db-migrate.yml` (applique migrations prod sur merge main). **Sans migration.** | fix/prod-hardening | 2026-06-17 | actif |
| Claude (qualiopi-rnq) | Référentiel Qualiopi officiel versionné (RNQ V9, puis V10 à 33 indicateurs au 1/11/2026) + moteur d'évaluation par vérifications + page Qualiopi façon mini-audit (onglets par critère, fiche par indicateur, preuves, applicabilité) : **migration 0139+**, `features/qualiopi/**`, `app/(dashboard)/qualiopi/**`, `app/(dashboard)/dossiers/[id]/qualiopi/**`, `features/dossier/qualiopi-guidance.ts`. Livré : migrations 0139 à 0143, page qualité, export d'audit, tableau de bord de session. SQL `supabase/A_APPLIQUER.sql` à jouer en prod. | (push direct main) | 2026-09-11 | fait |
| Claude (amelioration-continue) | Amélioration continue façon Digiforma : **migration 0144** (`quality_incidents`, `improvement_axes`, liens `incident_id`/`axis_id` sur `improvement_actions`), `app/(dashboard)/amelioration-continue/**` (onglets axes / incidents / actions / veille, actions gardées et bornées à l'organisation), `features/amelioration/**`, preuves I31/I32 dans `features/qualiopi/evidence.ts`. Reprend la zone « veille-amelioration » (inactive depuis juin, accord Ismael). | (push direct main) | 2026-09-11 | fait (migration 0144 à appliquer) |
| Claude (emargement-sosafe) | **ZONE CHAUDE** Refonte de l'émargement sur le modèle SoSafe : réparation de la signature (jetons jamais enregistrés, événement invalide), entrée + sortie, statuts d'absence, signature formateur et verrouillage, grille admin, modes tablette / QR / visio, envoi automatique des liens (désactivé par défaut), PDF et heures ; puis mode Edusign (QR dynamique projeté, écran en direct du formateur, justificatifs d'absence). **Migrations 0145+**, `features/attendance/**`, `app/(dashboard)/**/emargements/**`, `app/(apprenant)/signer/**`, `app/(formateur)/emarger/**`, `app/api/attendance/**`, `app/api/emargements/**`, justificatifs (`app/api/signer/**`, `app/(apprenant)/espace/[token]/sessions/**`), projection `app/(projection)/**`. | (push direct main) | 2026-09-11 | terminé (0145→0149 poussées) |
| Claude (espace-formateur) | **ZONE CHAUDE** Espace formateur complet : accès du formateur externe à ses séances (0150, politiques de lecture additives + fonctions `my_trainer_*`), émargement depuis son espace, planning + agenda ICS, puis questionnaires, évaluations, factures d'honoraires, notes de frais, documents. **Migrations 0150+**, `app/(formateur)/**`, `features/trainer-space/**`, `features/identity/trainer-self/ui/**`, `app/api/formateur/**`, `features/attendance/access.ts`. | (push direct main) | 2026-09-11 | actif |
| Claude (session-rfc) | **ZONE CHAUDE — demande explicite d'Ismael (« je reprends la main »)** : fiche session façon RFC (en-tête sobre + fil d'Ariane + étapes, onglets Informations · Participants · Émargement · Fiches besoin · Automatisations · Documents · Évaluations puis Heures · Qualiopi · Facturation · Financeurs · Dépenses · Activité · Accès), grille d'émargement façon RFC, page Émargements cliquable ; retrait de la barre d'onglets du haut (`section-tabs`, doublon de la sidebar, jugée inutile par l'utilisateur). `app/(dashboard)/sessions/[id]/**`, `app/(dashboard)/sessions/page.tsx` (graphiques en haut), `app/(dashboard)/emargements/**`, `app/(dashboard)/layout.tsx`. **Migrations 0155** (capacité de séance) **et 0156** (automatisations par séance). Livré en plus : fiches besoin visibles dans la séance et dans l'espace formateur (`features/questionnaire/session-needs.ts`, `app/(formateur)/seance/[id]/fiches-besoin/**`), fiche d'identité complète sur la fiche formation, documents par stagiaire dans l'onglet Documents (convocation PDF, e-mail, mise en signature, aperçu PDF sur la page de signature), et chaque envoi automatique coupable séance par séance (crons `transactional-emails` et `emargement-liens` s'y conforment). | (push direct main) | 2026-09-12 | fait |
| Claude (charte-tech) | Charte graphique v3.1 « tech & moderne » (même DA orange/violet) : polices Geist Sans/Mono (`app/layout.tsx`), ombres en couches (`tailwind.config.ts`), `globals.css`, shell (`shared/components/layout/{sidebar-rail,topbar}.tsx`, `(dashboard)/layout.tsx`), primitives `shared/ui/{button,status-pill,section-label}.tsx`, page `sessions/page.tsx`, charte `.cursor/rules/70-ui-charter.mdc`. Balayage transverse **classes seulement** : `font-mono` → `tabular-nums` sur dates/heures/montants (~50 fichiers). **Sans migration.** | (push direct main) | 2026-09-11 | fait |
| Claude (devis-facturation-rfc) | Devis & facturation façon RFC + distinction entreprise / particulier (demande Ismael) : **migration 0152+** (`quotes`, `quote_lines`, `quote_dossiers`, `document_counters` + RPC de numérotation, `sessions.price_cents`, `invoices.quote_id`), `features/billing/**`, `app/(dashboard)/devis/**`, `app/(dashboard)/factures/**`, `app/(dashboard)/dossiers/[id]/facturation/**`, `app/(dashboard)/sessions/[id]/facturation/**`, `features/dossier/load-progress.ts`, conversion prospect (`features/crm/prospect-conversion/convert-core.ts`), puis conventions/contrat particulier, convocations, feuilles d'émargement par entreprise. | (push direct main) | 2026-09-11 | fait — suite livrée : avoirs, acompte/solde, relances automatiques, feuille de présence signée par entreprise, certificat à l’entreprise (migration 0154+, `features/attendance/pdf-render.tsx`, `app/api/dossiers/[id]/certificat.pdf`) |
| Claude (charte-v4) | **Charte v4 « Horizon couleur » sur tout le CRM** (validée par l'utilisateur) : jetons globaux (`tailwind.config.ts` : zinc → gris bleu nuit, violet → orange, police Manrope via `app/layout.tsx`), `globals.css` (couleurs de formation), shell (`sidebar-rail`, `topbar`, nouveau `section-tabs`, `(dashboard)/layout.tsx`), primitives `shared/ui/**`, `shared/lib/formation-color.ts`, page `sessions/page.tsx` (une ligne par session : Formation, Formateur, Dates, Lieu, Participants, Statut, Actions), puis restyle écran par écran (**classes seulement**, hors zones claimées : factures, devis, espace formateur). **Sans migration.** | (push direct main) | 2026-09-11 | fait (hors factures, devis, espace formateur, émargements, inscription) |

| Claude (mentions-documents) | **Mentions identiques sur tous les documents** (demande Ismael, modèle RFC) : bloc d'identité de l'organisme (nom, adresse, téléphone | e-mail, SIRET · déclaration d'activité · agréments type CNAPS) + mention RGPD unique en pied, appliqués à TOUS les générateurs — `features/documents/legal/**`, `features/documents/generate-*-pdf.ts`, `features/documents/templates/wrap-generated-html.ts`, `features/attendance/{pdf-render,paper-sheet-pdf,qr-cards-pdf}.tsx`, champ « agréments » dans `parametres/organisation/**` + migration. Puis GED versionnée (une entrée par document, dernière version + historique, documents régénérés à l'ouverture). | (push direct main) | 2026-09-12 | fait |
| Claude (charte-v4) | Fix aperçu document : `app/api/documents/[id]/route.ts` relaie le fichier depuis notre domaine (au lieu de rediriger vers l'URL signée du stockage — Safari n'affichait pas le PDF dans le cadre intégré, aperçu blanc) ; `?dl=1` force le téléchargement ; `documents/[id]/apercu/page.tsx` : `<object>` + lien de repli. **À connaître pour la GED versionnée.** | (push direct main) | 2026-09-12 | fait |

> Convention numéros de migration : avant d'écrire `supabase/migrations/NNNN_*.sql`,
> prendre `(dernier numéro sur origin/main) + 1` au moment du push, pas du brainstorm.
